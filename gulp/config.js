var build = './build/';
var src = './src/';
var assets = 'assets/';

var paths = {
  src: src,
  build: build,

  assets: assets,
  img: assets + 'img/',
  js: assets + 'js/',
  scss: assets + 'scss/',
  css: assets + 'css/',

  content: 'content/',
  layouts: 'layouts/',
  partials: 'partials/',
  templates: 'templates/'
};

module.exports = {
  paths: paths
}
